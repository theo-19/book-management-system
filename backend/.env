   Create a `.env` file in both `backend` and `frontend` folders with the following keys:

   ```env
   # backend/.env
   # Generate a 256‑bit (32 byte) secret and paste it here:
   # e.g. run in your terminal:
   #   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   JWT_SECRET=<paste‑your‑32‑byte‑hex‑string‑here>
   PORT=3001
